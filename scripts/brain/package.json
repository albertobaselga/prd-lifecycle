{
  "name": "prd-lifecycle-brain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node18 --outfile=dist/brain.cjs --format=cjs --external:fsevents",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "xstate": "^5.19.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "vitest": "^2.1.0"
  }
}
